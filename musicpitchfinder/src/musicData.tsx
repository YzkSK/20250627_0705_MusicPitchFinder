export type musicData = {
    id: string,
    created_at: EpochTimeStamp,
    title: string,
    original_url: string,
    cover_url: string,
    high_pitch: number,
    low_pitch: number,
    optimal_pitch: number,
    profiles_id: string,
}
